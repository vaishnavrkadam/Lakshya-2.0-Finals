rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Athlete photos are publicly readable, uploadable only by authenticated admin
    match /athletes/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
